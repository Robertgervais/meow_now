%h1
  Current Roles

%table.table.table-bordered.table-striped
  %thead.thead-light
    %tr
      %th
        Role
      %th
        Description
    - @roles.each do |role|
      %tr
        %td
          = role.name
        %td
          = role.description
