<li class="awaiting-ballot" id="<%= ballot.id %>"> <%= link_to "#{ballot.ballot_issue}", ballot_path(ballot)%></li>
<!-- 	<ul>
		<% find_users_that_have_not_voted(ballot.id).each do |user| %>
			<li> <%= user.username %></li>
		<% end %>
	</ul>
 -->